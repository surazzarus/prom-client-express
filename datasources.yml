apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    # url: http://docker.for.mac.host.internal:9090 ### Uncomment For MAC
    url: localhost:9090 ### Uncomment For Linux
    basicAuth: false
    isDefault: true
    editable: true